{
  "configurations": [
    {
      "name": "Love2D",
      "type": "lua",   // Lua 디버거가 아니라 단순 실행용
      "request": "launch",
      "program": "C:/Program Files/LOVE/love.exe",
      "args": ["${workspaceFolder}"],
      "cwd": "${workspaceFolder}"
    }
  ]
}